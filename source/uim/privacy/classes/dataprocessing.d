
module uim.privacy.classes.dataprocessing;

import uim.privacy;

// @Deprecated
class DDSGDataProcessing { // Datenverarbeitung
/* : CnATreeElement 
	: IDatenschutzElement {
	// ID must correspond to entity definition in XML description
	static final string TYPE_ID = "datenverarbeitung";
 */	
	
	
	/**
	 * Create new BSIElement.
	 * @param parent
	 */
/* 	this(CnATreeElement parent) {
		super(parent);
		setEntity(new Entity(TYPE_ID));
		getEntity().initDefaultValues(getTypeFactory());
	}
	
	this Datenverarbeitung() {
	}
	
	override string getTitle() {
		return getEntityType().getName();
	}

	override
	boolean canContain(Object obj) {
		return false;
	}
 */
}
